{
  "name": "chinese-jfm",
  "version": "2.0.0",
  "description": "Source files to generate the latex package chinese-jfm",
  "main": "index.js",
  "scripts": {
    "dev": "tstl --watch",
    "build": "pwsh .\\script\\clean.ps1 -dist -test && tstl && pwsh .\\script\\copy.ps1",
    "clean dist": "pwsh .\\script\\clean.ps1 -dist",
    "clean test": "pwsh .\\script\\clean.ps1 -test",
    "copy": "pwsh .\\script\\clean.ps1 -test && pwsh .\\script\\copy.ps1"
  },
  "keywords": [],
  "author": "ListLee",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^18.15.10",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.2",
    "typescript-to-lua": "^1.14.0"
  }
}
